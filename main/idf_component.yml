## IDF Component Manager Manifest File
dependencies:
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  joltwallet/littlefs:
    version: "~=1.20.0"
    rules:
      - if: "idf_version >=5.0"
